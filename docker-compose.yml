services:
  vertx-app:
    image: sample/vertx-java-fat
    build:
      context: .
    ports:
      - "8880-8883:8080"
    networks:
      - vertx-network

networks:
  vertx-network:
    driver: bridge
