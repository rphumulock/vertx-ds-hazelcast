services:
  vertx-instance1:
    image: sample/vertx-java-fat
    build:
      context: .
    ports:
      - "8888:8080"
    networks:
      - vertx-network
    environment:
      - VERTX_PORT=8080
      - HAZELCAST_CLUSTER=true

  vertx-instance2:
    image: sample/vertx-java-fat
    build:
      context: .
    ports:
      - "8889:8081"
    networks:
      - vertx-network
    environment:
      - VERTX_PORT=8081
      - HAZELCAST_CLUSTER=true

  vertx-instance3:
    image: sample/vertx-java-fat
    build:
      context: .
    ports:
      - "8890:8082"
    networks:
      - vertx-network
    environment:
      - VERTX_PORT=8082
      - HAZELCAST_CLUSTER=true

networks:
  vertx-network:
    driver: bridge
